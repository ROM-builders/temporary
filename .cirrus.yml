test_task:
  container:
    image: apon77/aosp:ccache
  test_script:
    - ./ccache -V
    - which ccache
    - sleep 1m
task:
  name: Build
  depends_on:
    - test
  persistent_worker:
    isolation:
      container:
        image: apon77/aosp:ccache
        cpu: 8
        memory: 40g
        volumes:
          - /home/cirrus/roms:/tmp/roms
  check_script:
    - sleep 2

